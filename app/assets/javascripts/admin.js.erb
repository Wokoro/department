$(document).ready(function(){
    //////////////////////////////////////////////////////////////////////////////////////////////////////////////
    ////////////////    Note!!!! Ajax was done with rails helpers ie, setting remote to true    //////////////////
    /////////////////////////////////////////////////////////////////////////////////////////////////////////////

        //enable loader for ajax start and finish events
        /*
        $(document).ajaxStart(function () {
            $("#loader").addClass("loader");
        }).ajaxStop(function () {
            $("#loader").removeClass("loader");
        });
        */

/*
     $("body").click(function(event){
         var x = event.target.id;
         var ref = $("#"+x).attr("data-ref");
         if (ref){
            $("[data-class=display]").each(function(){
                $(this).fadeOut(1);
            });

            $("[data-class=remove]").each(function(){
                    $(this).remove();
            });

            $("#"+ref).fadeIn(50);
         }
     });

*/

	$("body").click(function(event){
         var x = event.target.id;
         var data = $("#"+x).attr("data-class");
         if (data && data == "cancel_ref"){
			var ref = $("#"+x).attr("data-ref");
            $("#"+ref).css("display", "none");
			
			$("#delete_lecturer_form_fields").css("display", "inline");
			$("#cancel_edit_lect_form").text("Ok");
			$("#edit_lecturer_form_fields").css("display", "inline");
			$('input[id*="elete_"]').css("display", "inline-block");
			$("#edit_lect_field").css("display", "inline-block");
         }
     });
	 
	 $("li[class='tab-title']").click(function(event){
		var elementId = event.target.id;
		/* $("li[class='tab-title']").each(function(){
			$(this).css("display", "none");
		}); */
		
		alert(elementId);
	 });
	 	
});

});
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////   User defined functions  //////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////
