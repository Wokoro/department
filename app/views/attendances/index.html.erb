<% @courses.each do|course| %>
	<%= form_for :attendance, method:"get", url:print_attendance_path do |f| %>
		<%= course.ccode %>
		<%= course.ctitle %>
		<%= f.hidden_field :course, value:"#{course.ccode}" %>

		<%= submit_tag "Print Attendance" %>
	<% end %>
<% end %>