        <!-- The main content starts here-->
        <div class = "row">
          <div class = "col-2"></div>
                <div class = "col-4 no-padding ">
                    <!-- The login form -->
                    <div id="ccl">
                        <div id="hf">
                            <p id="ft">Staff Login</p>
                                <div id="hsf">
                                        <%= form_for :lecturer, url: lecturers_path do |f| %>
                                            <%= f.label :staff_id, id: 'staff_id' %>
                                            <%= f.text_field :staff_id %><br/>
                                            <%= f.label :password %>
                                            <%= f.password_field :password %><br/>
                                            <%= submit_tag :CREATE %>
                                        <% end %>
                                </div>
                        </div>
                    </div>
                </div>
          <div class = "col-2"></div> 
        </div>